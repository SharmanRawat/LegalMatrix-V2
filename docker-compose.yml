services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    volumes:
      - ollama_data:/root/.ollama
    ports:
      - "11434:11434"
    command: serve
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: backend
    environment:
      - OLLAMA_HOST=http://ollama:11434
      - TRANSFORMERS_TRUST_REMOTE_CODE=1
      # Credential rotation: read from ./docker-compose.env or the shell env.
      # Defaults are the demo fallback — set real values for any shared demo.
      - LEGALMATRIX_AUTH_SECRET=${LEGALMATRIX_AUTH_SECRET:-change-me-in-production-legalmatrix}
      - LEGALMATRIX_ADMIN_USERNAME=${LEGALMATRIX_ADMIN_USERNAME:-admin}
      - LEGALMATRIX_ADMIN_PASSWORD=${LEGALMATRIX_ADMIN_PASSWORD:-admin@123}
    ports:
      - "8000:8000"
    volumes:
      - legalmatrix_data:/app/backend/data
      - ./images:/app/images
    depends_on:
      - ollama
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        API_UPSTREAM: http://backend:8000
    container_name: frontend
    environment:
      - API_UPSTREAM=http://backend:8000
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  ollama_data:
  legalmatrix_data: